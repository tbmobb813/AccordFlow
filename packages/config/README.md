# @accordflow/config

Shared configuration for the AccordFlow monorepo.

Includes:

- ESLint shared config in `eslint/index.js`
- TypeScript TSConfig presets in `typescript/`

Install or reference these files from workspace packages to keep linting and TypeScript settings consistent.
